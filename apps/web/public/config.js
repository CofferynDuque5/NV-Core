// NV Core — configuración en tiempo de ejecución (se puede editar SIN recompilar).
//
// Pon la URL pública de tu API (por ejemplo, tu servicio de Render) para conectar
// el front con el backend:
//
//   window.__NV_CONFIG__ = { apiUrl: "https://nv-core.onrender.com" };
//
// Déjalo vacío ("") para modo DEMO (la web se ve, pero sin login ni datos reales).
// Si el front y la API están en el MISMO dominio, usa "same-origin".
window.__NV_CONFIG__ = { apiUrl: "" };
