{
  "name": "@nv/api",
  "version": "0.0.0",
  "private": true,
  "description": "NV Core backend (NestJS) — PLACEHOLDER. Not implemented in phase 1.",
  "scripts": {
    "build": "echo \"(api) placeholder — NestJS not implemented yet\"",
    "dev": "echo \"(api) placeholder — run phase 2 to enable\"",
    "lint": "echo \"(api) placeholder\"",
    "typecheck": "echo \"(api) placeholder\""
  }
}
